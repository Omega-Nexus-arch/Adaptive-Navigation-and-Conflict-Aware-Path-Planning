# Two robots at the same priority make the yield decision depend on message
# arrival order. Must be rejected at load time.
fleet:
  model_library: "models_good.yaml"
  robots:
    - {name: "amr1", model: "heavy_test", yield_priority: 42}
    - {name: "amr2", model: "light_test", yield_priority: 42}
